name: detmeter
description: Compare and measure SIEM detection effectiveness
version: 1.0.0
access:
  - admin
  - red
  - blue
data:
  gui_path: /detmeter
  icon: chart-bar
  weight: 100
routes:
  - path: /detmeter
    name: DetMeter
    component: DetMeter
    access: [admin, red, blue]
  - path: /detmeter/api/analyze
    name: Analyze Operation
    component: API
    access: [admin, red]
  - path: /detmeter/api/status
    name: SIEM Status
    component: API
    access: [admin, red, blue]
